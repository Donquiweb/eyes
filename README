# eyes
**eyes** is an OpenCV project

It currently features the following programs:

- objectTracker: Ability to track objects, provided you use the filter settings to filter out the specific object's colour out.

- stereoVision: Using two webcams, it currently is only able to display two webcam feeds both at the same time.
